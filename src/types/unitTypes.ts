export enum DistanceUnits {
    mm,
    cm,
    m,
    km,
    inch,
    feet,
    yard,
    mile,
}

export enum TemperatureUnits {
    C,
    F,
}

export enum WeightUnits {
    lb,
    g,
    kg,
    st,
    oz,
}
